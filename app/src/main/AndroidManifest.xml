<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tdc">

    <!-- Permiso para usar Internet (Firebase lo requiere) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TDC">

        <!-- Actividad de inicio -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Otras actividades -->
        <activity android:name=".RegisterActivity" android:exported="false" />
        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".OrdenesActivity" android:exported="false" />
        <activity android:name=".AvisoActivity" android:exported="false" />
        <activity android:name=".AñadirAvisoActivity" android:exported="false" />
        <activity android:name=".AvisosGuardadosActivity" android:exported="false" />
        <activity android:name=".TrabajoEspecialActivity" android:exported="false" />
        <activity android:name=".TrabajosEspecialesGuardadosActivity" android:exported="false" />

    </application>
</manifest>
