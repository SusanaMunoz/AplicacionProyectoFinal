// build.gradle.kts (Proyecto)
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.1.0") // Actualiza a una versión más reciente
        classpath("com.google.gms:google-services:4.4.2")
    }
}

allprojects {
}
